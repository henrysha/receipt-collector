{
  "name": "receipt-collector",
  "version": "1.0.0",
  "description": "collect receipts for subscription services",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:netflix": "cypress run -s cypress/e2e/netflix.cy.ts",
    "cypress:google": "cypress run -s cypress/e2e/google.cy.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.11.0"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
